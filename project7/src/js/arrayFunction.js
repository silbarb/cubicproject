function findlength(sample){
    return sample.length;
}

function traverseArray(sample){
   // replace sampleArray with sample;
    traverseArray(sampleArray)
    for(var i=0; i<sample.length;i++){
    document.write(i+")"+sample[i]+"<br>");
        
}
    
}